name: ids_list
description: Test speed of various storages
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  cupertino_icons: ^1.0.2
  equatable: ^2.0.3
  flutter:
    sdk: flutter
  get_it: ^7.2.0
  hive: ^2.2.1
  meta: ^1.7.0
  path: ^1.8.0
  shared_preferences: ^2.0.15
  sqflite: ^2.0.2+1


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
